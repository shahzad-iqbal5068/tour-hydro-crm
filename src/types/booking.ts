/**
 * Booking types (star bookings, group bookings).
 * Form-specific types (e.g. BookingFormValues) may live in components;
 * this file holds shared booking enums and row shapes used across the app.
 */

/** Star booking category filter / form value */
export type StarBookingCategory = "4-5" | "3" | "all";
